# Claude Install

이 디렉토리는 Claude 웹 앱에 업로드할 ZIP 번들의 기준 파일입니다.

## Install

1. `targets/claude/harness-init/` 디렉토리를 ZIP으로 묶습니다.
2. Claude의 Skills 화면에서 업로드합니다.
3. 업로드 후 skill을 활성화합니다.

## Notes

- `SKILL.md`, `references/`, `scripts/`는 모두 함께 포함해야 합니다.
- 이 번들은 `skill/`에서 자동 생성되므로 직접 수정하지 않습니다.
